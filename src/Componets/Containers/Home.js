// The HomePage Container
// I changed my code to make it so much more simpler in the container 

import HomeView from "../views/HomeView";

const Home= () => {
    return (<HomeView/>);
};

export default Home;