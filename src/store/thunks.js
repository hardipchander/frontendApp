// Need all the action creators because they will be called inside the thunks
import * as actionCreators from "./actions/allActionCreators.js";

// I don't have my API set up yet need to wait on that 
let linkToAPI="";